/**
 * Build-time constant injected by the Angular `define` option (angular.json).
 * `false` for the production build so every `if (COLOSSUS_PREVIEW)` branch is
 * dead-code-eliminated; `true` for the `mockup` configuration that produces the
 * static, backend-less preview bundle.
 */
declare const COLOSSUS_PREVIEW: boolean;
